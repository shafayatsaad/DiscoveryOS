"use client";

// Purpose: Centralize Framer Motion primitives used by the landing page.

import { motion } from "framer-motion";

export const MotionDiv = motion.div;
export const MotionSection = motion.section;

